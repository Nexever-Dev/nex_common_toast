import 'package:flutter_test/flutter_test.dart';

void main() {
  test('adds one to input values', () {
    // Set the context for the singleton
    // ContextManager().setContext(context);
  });
}
